mutation UPDATE_SHIPPING_METHOD($checkoutId: ID!, $shippingMethodId: ID!) {
  checkoutShippingMethodUpdate(
    checkoutId: $checkoutId
    shippingMethodId: $shippingMethodId
  ) {
    checkoutErrors {
      field
      message
    }
  }
}
