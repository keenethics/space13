import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  main: {
    flex: 1,
    alignSelf: 'stretch',
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
})
