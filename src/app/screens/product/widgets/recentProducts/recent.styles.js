import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  root: {
    marginTop: 16,
  },
  title: {
    fontWeight: '600',
    marginBottom: 16,
    fontSize: 16,
    lineHeight: 24,
    textAlign: 'left',
  },
})
