mutation profile($input: AccountInput!) {
  accountUpdate(input: $input) {
    user {
      id
      email
      firstName
      lastName
      role
      phone
      birthday
      gender
      avatar {
        url
      }
    }
    accountErrors {
      message
    }
  }
}
